cd backend
python -m uvicorn api.main:app --reload --host 0.0.0.0 --port 8000
----Front-----
npm run dev



--------CTREAR DB USE POWERSHELL ADMINISTRADOR----
# Crear la base de datos
sqlcmd -S "STEVEN-ALIENWAR\SQLTRABAJO" -E -Q "CREATE DATABASE FraudDetectionDB"

# Si ya existe, te dirá que ya existe (está bien)

# Verificar que existe
sqlcmd -S "STEVEN-ALIENWAR\SQLTRABAJO" -E -Q "SELECT name FROM sys.databases WHERE name = 'FraudDetectionDB'"




----------DESACTIVAR ENTORNO------------------
# Primero desactiva el entorno actual
deactivate

-----------Activar ENTORNO-----------
# Ahora activa el entorno de 32 bits
.\venv32\Scripts\Activate.ps1
depedencias32:
pip install fastapi uvicorn sqlalchemy pyodbc python-dotenv pydantic websockets
# Ahora activa el entorno de 64 bits
.\venv\Scripts\Activate.ps1


----------NO OLVIDAR CAMBIAR EL INTERTREPE EN EL TERMINAL A 32 O 64------------------
.\venv32\Scripts\python.exe
.\venv\Scripts\python.exe

---------Dependencias-------
# Instalar requests en tu entorno virtual activo
pip install requests

--------DATO------------
El sistema mantendrá total compatibilidad con tu código existente mientras te
permite agregar nuevos detectores simplemente creando nuevos archivos *_detector.py en el directorio de detectores.
